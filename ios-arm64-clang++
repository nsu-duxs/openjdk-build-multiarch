set -e
$thecxx -arch arm64 -isysroot $thesysroot $@
# -Wl,-arch,arm64
#,-isysroot,$thesysroot $@
#$(xcrun -find -sdk iphoneos clang++) -arch arm64 -isysroot $(xcrun --sdk iphoneos --show-sdk-path) $@

